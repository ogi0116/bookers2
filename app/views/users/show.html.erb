<div> <%= flash[:notice] %> </div>

<div class = "container">
  <div class = "row">
    <div class = "col-md-3">
      <!--部分テンプレート入り1：ユーザ情報、ユーザ編集リンク(Userフォルダーに保存)-->
      <%= render 'userinfo', user: @user %>
      <!--ここまでがユーザフォルダーに保存するテンプレート-->

      <!--部分テンプレート入り2：新規投稿フォーム(bookフォルダーに保存)-->
      <%= render 'books/newbook', book: @book %>
      <!--部分テンプレート終わり2：新規投稿フォーム(bookフォルダーに保存)-->
    </div>

    <div class = "col-md-8 offset-md-1">
      <table class="table table table-user">
        <h1>Books</h1>
        <thead>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
        </thead>

        <!--ユーザー(個人)の投稿一覧-->
        <!--画像のリンク先：現在地　タイトルのリンク先：本の詳細画面(books_path:id)-->
        <tbody>
          <% @books.each do |book| %>
          <tr>
            <td>
              <%= link_to user_path(book.user) do %>
               <%= image_tag book.user.get_profile_image(100,100) %>
             <% end %>
            </td>

            <td>
              <%= link_to book.title, book_path(book.id)  %>
            </td>

            <td>
              <%= book.body %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
  <!--ユーザー(個人)の投稿一覧終わり-->